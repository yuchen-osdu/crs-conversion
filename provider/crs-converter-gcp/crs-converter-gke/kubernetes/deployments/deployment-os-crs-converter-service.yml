apiVersion: v1
kind: Service
metadata:
  name: os-crs-converter-service
  labels:
    app: os-crs-converter-service
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8080
  selector:
    app: os-crs-converter-service
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: os-crs-converter-service
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: os-crs-converter-service
        version: v1
    spec:
      containers:
        - name: os-crs-converter-service
          envFrom:
            - configMapRef:
                name: os-service-config
          image: gcr.io/opendes/os-crs-converter-app
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
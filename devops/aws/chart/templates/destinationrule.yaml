{{- template "common.destinationrule" (list . "os-crs-conversion.destinationrule") -}}
{{- define "os-crs-conversion.destinationrule" -}}
## Define overrides for the service destination rule resource here
{{- end -}}
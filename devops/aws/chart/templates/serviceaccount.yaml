{{- template "common.serviceaccount" (list . "os-crs-conversion.serviceaccount") -}}
{{- define "os-crs-conversion.serviceaccount" -}}
## Define overrides for the service's service account resource here
{{- end -}}